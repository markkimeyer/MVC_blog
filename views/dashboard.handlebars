{{#if logged_in}}
{{#each blogPosts}}
<div class = "card">
    <div class = "card-body">
        <h2 class="card-header">{{title}}</h2>
        </br>
        <h6 class="card-subtitle mb-2 text-muted">Created by: {{user.name}} </h6>
        <p class="card-text">
            
            {{text}}

        </p>
 </div>
        {{!-- <a href="#" class="btn btn-primary btn-disabled">Edit</a> --}}
   

{{!-- <h2> {{title}} by {{user_id}}</h2>
<h3> {{text}} </h3>
<h4> {{created_at}} </h4> --}}

</div>
{{/each}}



    <button class="btn btn-primary" type="button"><h3><i class="fas fa-plus"></i>           New Post</h3></button>

{{/if}}